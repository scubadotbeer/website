import React from 'react'
import Layout from '../components/Layout'

const NotFoundPage = () => (
  <Layout>
    <div>
      <h1>NOT FOUND</h1>
      <p>It looks like you are trying to access a page we haven't brewed yet!</p>
    </div>
  </Layout>
)

export default NotFoundPage
